server:
  port: 8080

spring:
  application:
    name: ai-patrol-backend
  datasource:
    url: jdbc:mysql://localhost:3306/ai_patrol?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
